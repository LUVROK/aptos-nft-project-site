import './Wrapper.scss';

const Wrapper = () => {
  return (
    <div className="Wrapper section">
      Wrapper<br />
      <button className='pixelBtn' type='button' title='Mint'>Mint the witch</button>
    </div>
  );
}

export default Wrapper;